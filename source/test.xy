float a = 3.14;
float b = -2.87;

stdOut("a: ");
stdOut(a);
stdOut("\nb: ");
stdOut(b);

stdOut("\na + b = ");
stdOut(a + b);
stdOut("\na - b = ");
stdOut(a - b);
stdOut("\na * b = ");
stdOut(a * b);
stdOut("\na / b = ");
stdOut(a / b);